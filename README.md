# notion-triage-agent
LLM-powered task triage agent that reads a Notion database, classifies items, extracts action items, assigns priority, and recommends what to work on next. Built with LangGraph, Pydantic structured output, and the Notion API.
